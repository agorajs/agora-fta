import { Node } from 'agora-graph';
/**
 * Returns a subset of v where each node is at the right of q
 * @param v set of nodes
 * @param q seed node
 */
export declare function R(v: Node[], q: Node): Node[];
